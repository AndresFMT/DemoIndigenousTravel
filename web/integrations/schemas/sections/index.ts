import { default as section } from './section';
import { default as sectionsreference } from './sections';

import { default as videoHero } from './videoHero';
import { default as textSection } from './textSection';
import { default as imageHero } from './imageHero';
import { default as videoSection} from './videoSection';

export const sections = [
  section,
  sectionsreference,
  videoHero,
  textSection,
  imageHero,
  videoSection,
];
